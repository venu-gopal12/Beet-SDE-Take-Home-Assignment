services:
  mongo:
    image: mongo:7
    container_name: beet-voice-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - beet_mongo_data:/data/db

volumes:
  beet_mongo_data:

